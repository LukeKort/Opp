# Objectives and constraints functions (May. 05, 2021)
import math

def objective(var):
    
    # Entre com a função objetiva após o "y".
    # Lembre-se de adicionar o número correto de variáveis
    # E fazer a ligação com var[i], sendo i = 0,1,2,...,n
    # Se precisar de ajuda, consulte a documentação no botão "Ajuda"

    x1 = var[0]
    x2 = var[1]

    y = 
    
    return y

def constraints(var):

    # Substitua "0" por uma função de restrição em yi
    # Você pode adicionar quantas funções de restrição desejar
    

    x1=var[0]
    x2=var[1]

    y1 = 0
    y2 = 0

    # Lembre-se de adicionar a condição abaixo e colocar "and" entre duas condições.

    if (y1 == 0) and (y2 == 0):
        return True
    else:
        return False